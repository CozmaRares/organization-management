{
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:client": "cd client && pnpm dev",
    "dev:server": "./php-server.sh",
    "build:compose": "node build-compose.js",
    "build": "docker compose up -d"
  }
}
