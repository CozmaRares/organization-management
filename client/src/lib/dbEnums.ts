export const clientContractType = Object.freeze([
  "permanent",
  "sezonier",
] as const);

export const clientContractStatus = Object.freeze([
  "acceptat",
  "standby",
  "respins",
  "suspendat",
] as const);
